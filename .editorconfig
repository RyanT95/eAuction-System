root = true

# Change indent size for code viewer to 4 instead of GitHub's default of 8
[*]
indent_style = tab
indent_size = 4
trim_trailing_whitespace = true

[*.md]
trim_trailing_whitespace = false
